const token = String(Math.floor(Math.random() * 1000000)).padStart(6, "0")


function getToken(){
    const token = String(Math.floor(Math.random() * 1000000)).padStart(6, "0")
    console.log(token)
}
// 함수(기능)를 사용하여 token의 결과값을 계속 다르게 보여준다 () 를 반듯이 붙여줘야함